const employee = {
    name: 'Imran nazir mim',
    designation: 'QA',
    salary: '20000',
    experience: 1
}
const keys = Object.keys(employee);//keys value
console.log(keys)
const values = Object.values(employee);
console.log(values)

//both all values
const entries = Object.entries(employee);
console.log(entries)//array inside array for 2d array

